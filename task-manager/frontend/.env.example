# Frontend Environment Variables

# Your authentication token (same as backend AUTH_TOKEN)
VITE_AUTH_TOKEN=your-secret-token-change-this-to-something-secure

# API URL (leave as /api for production, or set to your deployed backend URL)
# For local development with separate servers: http://localhost:3001
VITE_API_URL=/api
